# =============================================================================
# Core Dependencies (الاعتمادات الأساسية)
# =============================================================================
streamlit==1.46.0              # User interface (واجهة المستخدم)
pandas==2.3.1                  # Data processing (معالجة البيانات)
pytz==2024.1                   # Time zones (المناطق الزمنية)
pydantic==2.8.2                # Input validation and models (التحقق من المدخلات والنماذج)
python-dotenv==1.0.1           # Environment variables loading (تحميل متغيرات البيئة)
requests==2.32.4               # HTTP requests (طلبات HTTP)
argon2-cffi
# =============================================================================
# Web Scraping (استخراج البيانات من الويب)
# =============================================================================
beautifulsoup4==4.12.3         # HTML parsing (تحليل HTML)
lxml==5.2.2                    # Fast HTML/XML engine (محرك HTML/XML سريع)
greenlet==3.0.3                # Coroutine support (دعم المهام المتزامنة)
playwright==1.45.0             # Browser automation (التصفح الآلي)
redis==5.0.7                   # Caching (التخزين المؤقت)
nest_asyncio==1.6.0
# =============================================================================
# Plotting & Visualization (الرسم البياني والتصور)
# =============================================================================
plotly==6.2.0                  # Interactive charts (الرسوم التفاعلية)
# =============================================================================
# Error Reporting (إعداد التقارير)
# =============================================================================
sentry-sdk==2.8.0              # Error reporting to Sentry (إرسال الأخطاء إلى Sentry)
# =============================================================================
# Database (قاعدة البيانات)
# =============================================================================
psycopg2-binary==2.9.9         # PostgreSQL driver (مشغل PostgreSQL)
SQLAlchemy==2.0.31             # ORM and database management (ORM وإدارة قواعد البيانات)
# =============================================================================
# Testing (الاختبار)
# =============================================================================
pytest==8.3.2                  # Testing framework (إطار الاختبار)
pytest-mock==3.14.0            # Mocking for tests (المحاكاة للاختبارات)
pytest-asyncio==0.23.7         # Async testing support (دعم الاختبارات المتزامنة)
